<h2>Editing Book</h2>
<%= form_with model: @book, local:true do |f| %>
  <label for="title">Title</label>
  <%= f.text_field :title, id:"title" %>

  <label for="opinion">Opinion</label>
  <%= f.text_area :body, id:"opinion" %>

  <%= f.submit "Update Book" %>
<% end %>